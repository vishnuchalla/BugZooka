apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: bugzooka
    path: sidecar-patch.yaml

# Placeholder image name for the RAG sidecar; overridden via `kustomize edit set image`.
