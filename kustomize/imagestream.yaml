apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: bugzooka
  namespace: ${BUGZOOKA_NAMESPACE}
  labels:
    app: ${BUGZOOKA_NAMESPACE}
spec:
  lookupPolicy:
    local: true
  tags:
    - name: latest
      from:
        kind: DockerImage
        name: ${BUGZOOKA_IMAGE}
      importPolicy:
        scheduled: true
      referencePolicy:
        type: Source
